// Classe Libs.Data.Join

typedef struct JOINTABLE{
	void *Table;
	int Size;
}JoinTable;


/*
JoinTable *Join(void *table){
	JoinTable *Join = malloc(sizeof(JoinTable));
	Join->Table = table;
	Join->Size = sizeof(table);
	
	return Join;
}

*/
void getInfo(void *table){
		
//	FILE *pf;
//	JoinTable *medicos = Join(table);
	
//	char conteudo[100];

//	pf = fopen("arquivos/texto.txt", Mode.rb); // Abre o arquivo novamente para leitura

//	fread(&conteudo, sizeof(char), 100,pf); // Le em conteudo o valor da variável armazenada anteriormente pf

//	printf("\nO conteúdo do arquivo é':\n %s \n", conteudo);

//	fclose(pf);
}


//
